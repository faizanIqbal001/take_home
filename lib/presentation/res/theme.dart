

import '../export.dart';

final appTheme = ThemeData(
  // primarySwatch: MaterialColor(0xFF00c387, color),
  // accentColor: MaterialColor(0xFF00c387, color),
  visualDensity: VisualDensity.adaptivePlatformDensity,
);